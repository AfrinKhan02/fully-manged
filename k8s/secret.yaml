apiVersion: v1
kind: Secret
metadata:
  name: fully-managed-connectors-consumer-secrets
  labels:
    app: fully-managed-connectors-consumer
type: Opaque
stringData:
  # Replace with actual base64 encoded values or use sealed secrets / CI vars
  CLUSTER_API_KEY: "your_api_key"
  CLUSTER_API_SECRET: "your_api_secret"

